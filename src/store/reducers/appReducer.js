const initialState = {
    appName: 'Ferro ou Forra',
}

const appReducer = function(state = initialState, action) {
    switch(action.type) {   
        default:
            return state
    }
}
export default appReducer